import { ModalBox, ModalContainer, ModalHeader, RegistrationForm } from '../../shared/components';

export const RegistrationPage = () => (
  <ModalContainer>
    <ModalBox>
      <ModalHeader />
      <RegistrationForm />
    </ModalBox>
  </ModalContainer>
);

