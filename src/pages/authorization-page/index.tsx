import { AuthorizationForm, ModalBox, ModalContainer, ModalHeader } from '../../shared/components';

export const AuthorizationPage = () => (
  <ModalContainer>
    <ModalBox>
      <ModalHeader />
      <AuthorizationForm />
    </ModalBox>
  </ModalContainer>
);

